{
  "name": "7-segment-display-longest-word",
  "version": "0.0.0",
  "description": "",
  "main": "build/src/main.js",
  "types": "build/src/main.d.ts",
  "files": [
    "build/src"
  ],
  "license": "Apache-2.0",
  "keywords": [],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "check": "gts check",
    "clean": "gts clean",
    "compile": "tsc -p .",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run check"
  },
  "dependencies": {
    "@types/node": "^10.11.4",
    "ts-node": "^3.2.0",
    "typescript": "^2.4.1"
  },
  "devDependencies": {
    "gts": "^0.8.0",
    "typescript": "~2.8.0"
  }
}
